{
  "name": "all-kv-functions",
  "version": "1.0.0",
  "description": "Example of every KV function",
  "main": "index.js",
  "scripts": {
    "build": "npx webpack --mode=production && mkdir -p target && spin js2wasm -o target/all-kv-functions.wasm dist/spin.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-loader": "^9.4.1",
    "typescript": "^4.8.4",
    "webpack": "^5.74.0",
    "webpack-cli": "^4.10.0"
  },
  "dependencies": {
    "@fermyon/spin-sdk": "^0.5.0"
  }
}
